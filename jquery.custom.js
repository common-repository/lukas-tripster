(function( $ ) {
    $(function() {
         
        // Add Color Picker to all inputs that have 'color-picker-hex' class
        $( '.color-picker-hex' ).wpColorPicker();
         
    });
})( jQuery );
